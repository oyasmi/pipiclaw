# Operator incident RC-17

Task RC-17 is due for a scheduled wake. Its attempt budget is 3/3 consumed, and its deadline expired yesterday.

Question: should the runtime send RC-17 to the model now, or stop it before model work? Explain which current rule decides the outcome.
